apiVersion: v1
kind: Service
metadata:
  name: petclinic-frontend
  namespace: devops-lab
  labels:
    app: petclinic
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: petclinic
    component: frontend
